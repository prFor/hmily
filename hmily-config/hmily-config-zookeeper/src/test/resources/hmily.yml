hmily:
  server:
    configMode: zookeeper
    appName: xiaoyu
  #  如果server.configMode eq local 的时候才会读取到这里的配置信息.

remote:
  zookeeper:
    serverList: 192.168.3.21:2181
    fileExtension: yml
    path: /hmily/config
    passive: true

